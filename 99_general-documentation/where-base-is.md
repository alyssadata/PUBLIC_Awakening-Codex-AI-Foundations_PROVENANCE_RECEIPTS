Base = the first saved trained weight-state.

---

Base = original trained checkpoint.
Fine-tuned/post-trained model = base shaped further.
API = doorway to call the shaped model.
App = human interface around the API/model.

---

A base model is clay after the first firing.

Fine-tuning is sculpting or glazing that clay further.

The API is the counter where you interact with the finished object.